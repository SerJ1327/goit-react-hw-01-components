import styled from 'styled-components';

const STransTable = styled.table`
  width: 480px;
`;

const STransHeadTr = styled.tr`
  background-color: #00cccc;
  padding: 10px 0;
  height: 40px;
`;

const STransTd = styled.td`
  text-align: center;
  height: 30px;
`;

export { STransTable, STransHeadTr, STransTd };
